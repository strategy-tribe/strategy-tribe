// import { MapData } from '../models/map/types';

// export const useGetRegionsCountries = (data: MapData) => {
//   const getRegionCountries = (region: string) => {
//     const regionData = data.regions.find(
//       (regionData) => regionData.location === region
//     );
//     const countries = regionData?.countries;
//     return countries;
//   };

//   return {
//     getRegionCountries,
//   };
// };

export {};
